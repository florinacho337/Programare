USE Spotify

EXEC restoreVersion @version=3

EXEC restoreVersion @version=5

EXEC restoreVersion @version=2

EXEC restoreVersion @version=1

EXEC restoreVersion @version=4

EXEC restoreVersion @version=0

EXEC restoreVersion @version=-3

EXEC restoreVersion @version=6
